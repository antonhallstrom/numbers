describe('unitConverter', () => {
  it ('converts fahrenheit to degrees', () => {
    // arrange

    // act

    // assert
  })
  it ('converts degrees to fahrenheit', () => {
    // arrange

    // act

    // assert
  })
})
